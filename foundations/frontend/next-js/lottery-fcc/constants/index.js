const contractAddresses = require("./contractAddresses.json")
const abi = require("./abi.json")

// @ztmy this index.js represents this whole folder.
module.exports = {
    contractAddresses,
    abi,
}
